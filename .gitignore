# Build
/target
# Fixtures are standalone packages (excluded from the workspace); ignore their
# build output and lockfiles (no deps -> nothing to pin).
fixtures/*/target/
fixtures/*/Cargo.lock
fixtures/red-paths/f1.txt

# Transcript diagnostic dumps (raw bytes are for local diagnosis, not committed)
/gate-out/
*.bin

# Harness scratch
harness/**/hashes.json

# NOTE: goldens/ IS committed. It is the golden source of truth. Do not ignore.
